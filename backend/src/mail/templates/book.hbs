<p>Hello {{ participant.name }}!</p>

<p>You have received a new message from Apollusia.</p>

<p>The poll has concluded:</p>

<div style="border: 1px solid gray; border-radius: 5px; padding: 5px">
    <h3>{{ poll.title }}</h3>
    {{#if poll.location}}
        <h4>Location: {{ poll.location }}</h4>
    {{/if}}
    {{#if poll.description}}
        <h4>Description:</h4>
        {{markdown poll.description}}
    {{/if}}
</div>

<p>The following appointments have been booked:</p>
<ul>
    {{#each appointments}}
        <li>{{this}}</li>
    {{/each}}
</ul>
<p>You have agreed to appointments marked with *.</p>

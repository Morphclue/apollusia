import { LocationIconPipe } from './location-icon.pipe';

describe('LocationIconPipe', () => {
  it('create an instance', () => {
    const pipe = new LocationIconPipe();
    expect(pipe).toBeTruthy();
  });
});
